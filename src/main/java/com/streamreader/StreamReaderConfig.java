package com.streamreader;

import java.util.UUID;
import java.util.logging.Logger;

public class StreamReaderConfig {

	public static Logger LOGGER = Logger.getLogger(StreamReader.class.getName());
	public static UUID ID;
}
